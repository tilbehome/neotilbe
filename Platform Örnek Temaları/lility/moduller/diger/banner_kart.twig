<div class="image">{{ gorsel.lazy(banner.resim, '', banner.baslik_alt) }}</div>

<div class="banner-content">
    {% if modul.icerik_baslik %}
        <div class="title">{{ banner.baslik }}</div>
    {% endif %}
    {% if modul.icerik_aciklama %}
        <div class="description">{{ banner.aciklama }}</div>
    {% endif %}
    
    <div class="banner-btn">
        {{basHarfBuyukCevir('alisverise_basla')}}
    </div>
</div>