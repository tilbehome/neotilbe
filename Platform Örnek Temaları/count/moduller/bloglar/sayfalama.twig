{% extends varsayilanBlokModul() %}

{% block modulBaslik %}
    {{ sayfaBilgileri('baslik') }}
{% endblock %} 

{% block modulIcerik %}
    {% if sayfaBilgileri('icerik').icerik_sayisi > 0 %}
        <div class="row">
            {% for blog in sayfaBilgileri('icerik').liste %}
                <div class="{{ modul.listeleme_class }} col-list-p-v-1">
                    {% include modul.kart %}
                </div>
            {% endfor %}
        </div>

        <div class="pagination-wrapper">
            {% include modul.sayfalar  %}
        </div>
    {% else %}
        <div class="alert alert-warning">
            {{ cevir('blog_bulunamadi') }}
        </div>
    {% endif %}
{% endblock %} 